   git init
   git status
   git add .
   git commit -m ""
   git commit -a -m "last"
   git log
   git checkout --
   git reset head 
   git log --oneline

   git branch -a
   git checkout master
   touch .gitignor
   